# RSI-EAF Core Dependencies
# xrpl-py for all XRP Ledger interactions (wallets, payments, queries, WebSocket monitoring)
xrpl-py>=2.0.0

# Environment & config
python-dotenv>=1.0.0

# For future dashboard / web components (lightweight)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data & observability
pydantic>=2.8.0
pandas>=2.2.0   # For metrics analysis in later cycles

# Optional but recommended for rich market research / scraping in revenue engines
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Development / testing
pytest>=8.3.0
black>=24.8.0
ruff>=0.6.0
